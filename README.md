
  # Design UNICEF Standards

  This is a code bundle for Design UNICEF Standards. The original project is available at https://www.figma.com/design/lguLPVjCDH8rumsdc9gDW6/Design-UNICEF-Standards.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  